import { SliderightDirective } from './slideright.directive';

describe('SliderightDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderightDirective();
    expect(directive).toBeTruthy();
  });
});
