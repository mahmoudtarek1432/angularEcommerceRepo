import { BannerscrollDirective } from './bannerscroll.directive';

describe('BannerscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new BannerscrollDirective();
    expect(directive).toBeTruthy();
  });
});
