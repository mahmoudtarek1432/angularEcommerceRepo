import { NavbarscrollDirective } from './navbarscroll.directive';

describe('NavbarscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarscrollDirective();
    expect(directive).toBeTruthy();
  });
});
